<!DOCTYPE html>
<html>
	<head>
		<% include partials/head %>
	</head>
	<body>
		<div class="jumbotron" style="background-color:black; color:white!important">
			<div class="container">
				<h1>Lasers of Outer Space</h1>
				<p>	Online!</p>
			</div>
		</div>
		
		<div class="container-fluid">
			<div class="row">
				<div class="col-md-6 mid-divider"">
					<div>
						<h1 class="text-center">About the Game</h1>
						<p class="text-center">Shoot people with spaceships!</p>

					</div>
				</div>

				<div class="col-md-6 text-center">

					<%if(data.authenticated == false){%>
					<span class="glyphicon glyphicon-lock" aria-hidden="true"></span>

					<h2>Login to play!</h2>

					<% include partials/error_flash%>

					<form method = "post" action = "/"  style = "padding-left: 25%;width: 75%">
						<div class = "form-group">
							<label>Username</label>
							<input type = "text" class = "form-control" name="username" placeholder = "Username">
						</div>
						<div class = "form-group">
							<label>Password</label>
							<input type = "password" class = "form-control" name="password" placeholder = "Password">
						</div>
						<button type = "submit" class = "btn btn-default">Login!</button> <p>Dont have an account yet? <a href="/register">Click here to register!</a></p>
					</form>	
					<% } %>

					<%if(data.authenticated == true){%>

					<h2>Welcome <bold><%=data.username.toUpperCase()%></bold>!</h2>
					<form action="/game">
						<button type="submit" class = "btn btn-default"> PLAY </button>
					</form>
					<form action="/logout">
						<button type="submit" class = "btn btn-default"> LOGOUT </button>
					</form>
					<% } %>
				</div>

			</div>
		</div>

		<div style="background-color: black; padding: 2%; position:fixed; bottom:0%; width:100%"></div>
	</body>
</html>